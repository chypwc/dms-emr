# ./config/custom-pg_hba.conf

# Allow local Unix socket connections (e.g., from inside container)
local   all             all                                     trust

# Allow DMS / external TCP connections
host    all             all             0.0.0.0/0               md5